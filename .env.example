# =============================================================================
# Service Feature - Environment Configuration
# =============================================================================
# Copy this file to .env and configure the values
#
# cp .env.example .env
# =============================================================================

# =============================================================================
# LLM Provider Configuration (at least one required for code generation)
# =============================================================================

# Anthropic Claude (recommended)
ANTHROPIC_API_KEY=your-anthropic-api-key

# OpenAI (fallback)
# OPENAI_API_KEY=your-openai-api-key

# Google AI (fallback)
# GOOGLE_API_KEY=your-google-api-key

# Default provider: anthropic, openai, google
DEFAULT_LLM_PROVIDER=anthropic

# =============================================================================
# Git Authentication (required for private repositories)
# =============================================================================

# SSH Key Authentication
# GIT_SSH_KEY_PATH=/path/to/your/ssh/private/key

# HTTPS Authentication (GitHub/GitLab tokens)
# GIT_HTTPS_USERNAME=your-username
# GIT_HTTPS_PASSWORD=your-personal-access-token

# =============================================================================
# Service Configuration
# =============================================================================

# Log level: debug, info, warn, error
LOG_LEVEL=info

# Server address
SERVER_ADDRESS=:8080

# =============================================================================
# Sandbox Configuration
# =============================================================================

# Enable sandbox isolation (recommended)
SANDBOX_ENABLED=true

# Sandbox type: docker, gvisor, firecracker
SANDBOX_TYPE=docker

# Resource limits
SANDBOX_MEMORY_LIMIT_MB=2048
SANDBOX_CPU_LIMIT=1.0

# Network access in sandbox (disable for security)
SANDBOX_NETWORK_ENABLED=false

# =============================================================================
# Queue Configuration (defaults work for development)
# =============================================================================

# For production, use NATS or Kafka:
# QUEUE_FEATURE_REQUEST_URI=nats://nats:4222/feature.requests
# QUEUE_FEATURE_RESULT_URI=nats://nats:4222/feature.results

# =============================================================================
# Database Configuration (defaults work with docker-compose)
# =============================================================================

# DATABASE_URL=postgres://feature:feature@postgres:5432/feature?sslmode=disable

# =============================================================================
# Observability
# =============================================================================

METRICS_ENABLED=true
TRACING_ENABLED=true
# TRACING_ENDPOINT=jaeger:4317
